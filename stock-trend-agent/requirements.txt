# Stock Trend Agent — Python dependencies
# Install with: pip3 install -r requirements.txt
# No API keys needed. Everything runs locally.

# Market data — real-time prices, OHLCV (no API key needed)
yfinance==1.2.0

# Technical indicators — RSI, MACD, EMA, Bollinger Bands, ADX, Stochastic, ATR
# Computed locally. No API key. No rate limits. No quota.
ta==0.11.0

# Data manipulation
pandas==2.3.3
numpy==2.0.2

# Timezone support
tzdata==2026.2
